{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd backend && if [ ! -f /data/saem_pool.db ]; then cp data/saem_pool.db /data/saem_pool.db; fi && uvicorn api:app --host 0.0.0.0 --port $PORT"
  }
}